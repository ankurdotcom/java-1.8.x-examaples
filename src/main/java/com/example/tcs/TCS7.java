package com.example.tcs;

import java.util.Comparator;
import java.util.Map;
import java.util.TreeMap;

import com.example.bank.Employee;

public class TCS7 {
    public static void main(String[] args) {
        ////////// EmpList = 1000 employee objects
        
        // Sort Employees based on Salary , FIlter all employee salary greater than 10000

        //Create a stream out of EmpList
        // Apply Filter , Check if salary is  > 10000
        // a Create Tree Map


        // Map<Employee, Integer> emp = new TreeMap <> (Comparator.comparing (intermedite :: get));

        // emp.putAll (intermedite);
    }
}
